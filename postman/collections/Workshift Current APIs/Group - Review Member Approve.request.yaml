$kind: http-request
url: "{{baseUrl}}/api/v1/groups/{{groupId}}/members/{{pendingMemberId}}"
method: PATCH
headers:
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
body:
  type: text
  content: |-
    {
      "action": "APPROVE"
    }
order: 9000
